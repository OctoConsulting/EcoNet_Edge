/* currently unused, would be used to run ffmpeg in a c file */
/* however, optimizations for ffmpeg are not in running the commands, */
/* but rather in the encoding algorithms itself */

#include <stdio.h>

int main() {
  printf("heyy");

  return 0;
}